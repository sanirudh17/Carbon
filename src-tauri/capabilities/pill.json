{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pill",
  "description": "Capability for the snippet expansion pill window",
  "windows": ["pill"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
