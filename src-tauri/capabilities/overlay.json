{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the Quick Overlay popup window",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "drag:default"
  ]
}
